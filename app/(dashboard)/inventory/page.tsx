export default function InventoryPage() {
  return (
    <main className="mx-auto max-w-5xl p-6">
      <h1 className="text-2xl font-semibold">Inventory Ledger</h1>
      <p className="text-slate-600">Use API endpoint <code>/api/ledger</code> to record receive/transfer/issue/return/adjust transactions.</p>
    </main>
  );
}
